# 接口文档

> Base URL: `http://127.0.0.1:8000`
>
> 所有响应统一格式：`{ "code": int, "message": string, "data": any }`
>
> 鉴权方式：在请求头中携带 `Authorization: Bearer <access_token>`

---

## 目录

1. [系统接口](#1-系统接口)
2. [认证接口](#2-认证接口)
3. [用户管理接口](#3-用户管理接口)
4. [文章接口](#4-文章接口)

---

## 1. 系统接口

### 1.1 健康检查

- **方法**：`GET`
- **路径**：`/health`
- **鉴权**：无
- **入参**：无
- **出参**：

```json
{
  "code": 200,
  "message": "success",
  "data": { "status": "healthy" }
}
```

### 1.2 根路径

- **方法**：`GET`
- **路径**：`/`
- **鉴权**：无
- **入参**：无
- **出参**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "app": "FastAPI Admin System",
    "version": "1.0.0",
    "docs": "/docs"
  }
}
```

---

## 2. 认证接口

### 2.1 用户注册

- **方法**：`POST`
- **路径**：`/api/v1/auth/register`
- **鉴权**：无
- **入参**（JSON Body）：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| username | string | 是 | 用户名（3-50字符，不含空格） |
| email | string | 是 | 邮箱 |
| password | string | 是 | 密码（6-128字符） |

- **请求示例**：

```json
{
  "username": "testuser",
  "email": "test@example.com",
  "password": "password123"
}
```

- **出参**：

```json
{
  "code": 200,
  "message": "注册成功",
  "data": {
    "id": 2,
    "username": "testuser",
    "email": "test@example.com",
    "is_active": true,
    "roles": ["user"],
    "created_at": "2025-01-01T00:00:00",
    "updated_at": "2025-01-01T00:00:00"
  }
}
```

- **错误响应**：

| HTTP状态码 | code | message |
|------------|------|---------|
| 409 | 409 | 用户名已存在 |
| 409 | 409 | 邮箱已被注册 |
| 422 | 422 | 参数校验失败 |

---

### 2.2 用户登录（OAuth2 表单）

- **方法**：`POST`
- **路径**：`/api/v1/auth/login`
- **鉴权**：无
- **入参**（表单 `application/x-www-form-urlencoded`）：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| username | string | 是 | 用户名 |
| password | string | 是 | 密码 |

- **请求示例**：

```
POST /api/v1/auth/login
Content-Type: application/x-www-form-urlencoded

username=testuser&password=password123
```

- **出参**：

```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "access_token": "eyJhbGciOiJIUzI1NiIs...",
    "refresh_token": "eyJhbGciOiJIUzI1NiIs...",
    "token_type": "bearer"
  }
}
```

- **错误响应**：

| HTTP状态码 | code | message |
|------------|------|---------|
| 422 | 422 | 用户名或密码错误 |
| 422 | 422 | 账号已被禁用 |

---

### 2.3 用户登录（JSON）

- **方法**：`POST`
- **路径**：`/api/v1/auth/login/json`
- **鉴权**：无
- **入参**（JSON Body）：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| username | string | 是 | 用户名或邮箱 |
| password | string | 是 | 密码 |

- **请求示例**：

```json
{
  "username": "test@example.com",
  "password": "password123"
}
```

- **出参**：同 [2.2 用户登录](#22-用户登录oauth2-表单)

---

### 2.4 刷新 Token

- **方法**：`POST`
- **路径**：`/api/v1/auth/refresh`
- **鉴权**：无（使用 refresh_token）
- **入参**（JSON Body）：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| refresh_token | string | 是 | 刷新令牌 |

- **请求示例**：

```json
{
  "refresh_token": "eyJhbGciOiJIUzI1NiIs..."
}
```

- **出参**：

```json
{
  "code": 200,
  "message": "刷新成功",
  "data": {
    "access_token": "eyJhbGciOiJIUzI1NiIs...",
    "refresh_token": "eyJhbGciOiJIUzI1NiIs...",
    "token_type": "bearer"
  }
}
```

- **错误响应**：

| HTTP状态码 | code | message |
|------------|------|---------|
| 401 | 401 | 无效或过期的刷新令牌 |

---

### 2.5 获取当前用户信息

- **方法**：`GET`
- **路径**：`/api/v1/auth/me`
- **鉴权**：**是**（Bearer Token）
- **入参**：无
- **请求示例**：

```
GET /api/v1/auth/me
Authorization: Bearer eyJhbGciOiJIUzI1NiIs...
```

- **出参**：

```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "id": 2,
    "username": "testuser",
    "email": "test@example.com",
    "is_active": true,
    "roles": ["user"],
    "created_at": "2025-01-01T00:00:00",
    "updated_at": "2025-01-01T00:00:00"
  }
}
```

- **错误响应**：

| HTTP状态码 | code | message |
|------------|------|---------|
| 401 | 401 | Not authenticated |
| 401 | 401 | 无效或过期的访问令牌 |

---

## 3. 用户管理接口

> 所有用户管理接口均需要 **admin 角色** 鉴权。

### 3.1 获取用户列表

- **方法**：`GET`
- **路径**：`/api/v1/users`
- **鉴权**：**admin**
- **查询参数**：

| 参数 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|
| page | int | 否 | 1 | 页码（>=1） |
| page_size | int | 否 | 10 | 每页条数（1-100） |

- **请求示例**：

```
GET /api/v1/users?page=1&page_size=10
Authorization: Bearer <admin_token>
```

- **出参**：

```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "items": [
      {
        "id": 1,
        "username": "admin",
        "email": "admin@example.com",
        "is_active": true,
        "roles": ["admin"],
        "created_at": "2025-01-01T00:00:00"
      }
    ],
    "total": 1,
    "page": 1,
    "page_size": 10,
    "total_pages": 1
  }
}
```

---

### 3.2 创建用户

- **方法**：`POST`
- **路径**：`/api/v1/users`
- **鉴权**：**admin**
- **入参**（JSON Body）：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| username | string | 是 | 用户名 |
| email | string | 是 | 邮箱 |
| password | string | 是 | 密码 |

- **出参**：用户详情（同注册返回格式）

---

### 3.3 获取用户详情

- **方法**：`GET`
- **路径**：`/api/v1/users/{user_id}`
- **鉴权**：**admin**
- **路径参数**：`user_id`（int）
- **出参**：用户详情

- **错误响应**：

| HTTP状态码 | code | message |
|------------|------|---------|
| 404 | 404 | 用户不存在 |

---

### 3.4 更新用户

- **方法**：`PUT`
- **路径**：`/api/v1/users/{user_id}`
- **鉴权**：**admin**
- **入参**（JSON Body，所有字段可选）：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| email | string | 否 | 邮箱 |
| is_active | bool | 否 | 是否启用 |
| role_names | string[] | 否 | 角色名称列表 |

- **请求示例**：

```json
{
  "is_active": false,
  "role_names": ["editor", "user"]
}
```

- **出参**：更新后的用户详情

---

### 3.5 删除用户

- **方法**：`DELETE`
- **路径**：`/api/v1/users/{user_id}`
- **鉴权**：**admin**
- **出参**：

```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

---

## 4. 文章接口

### 4.1 获取文章列表

- **方法**：`GET`
- **路径**：`/api/v1/articles`
- **鉴权**：无（公开接口）
- **查询参数**：

| 参数 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|
| page | int | 否 | 1 | 页码 |
| page_size | int | 否 | 10 | 每页条数 |
| keyword | string | 否 | - | 标题/摘要关键字 |
| tag | string | 否 | - | 标签名称筛选 |

- **请求示例**：

```
GET /api/v1/articles?page=1&page_size=5&keyword=FastAPI&tag=Python
```

- **出参**：

```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "items": [
      {
        "id": 1,
        "title": "FastAPI 入门教程",
        "summary": "学习 FastAPI",
        "author_id": 2,
        "author_name": "testuser",
        "tags": ["Python", "FastAPI"],
        "comment_count": 3,
        "created_at": "2025-01-01T00:00:00",
        "updated_at": "2025-01-01T00:00:00"
      }
    ],
    "total": 1,
    "page": 1,
    "page_size": 5,
    "total_pages": 1
  }
}
```

---

### 4.2 获取标签列表

- **方法**：`GET`
- **路径**：`/api/v1/articles/tags`
- **鉴权**：无
- **出参**：

```json
{
  "code": 200,
  "message": "获取成功",
  "data": [
    { "id": 1, "name": "Python", "created_at": "2025-01-01T00:00:00" },
    { "id": 2, "name": "FastAPI", "created_at": "2025-01-01T00:00:00" }
  ]
}
```

---

### 4.3 获取文章详情

- **方法**：`GET`
- **路径**：`/api/v1/articles/{article_id}`
- **鉴权**：无
- **出参**：

```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "id": 1,
    "title": "FastAPI 入门教程",
    "content": "这是文章正文...",
    "summary": "学习 FastAPI",
    "author_id": 2,
    "author_name": "testuser",
    "tags": ["Python", "FastAPI"],
    "comments": [
      {
        "id": 1,
        "content": "好文章！",
        "article_id": 1,
        "author_id": 3,
        "author_name": "commenter",
        "created_at": "2025-01-01T00:00:00"
      }
    ],
    "created_at": "2025-01-01T00:00:00",
    "updated_at": "2025-01-01T00:00:00"
  }
}
```

- **错误响应**：

| HTTP状态码 | code | message |
|------------|------|---------|
| 404 | 404 | 文章不存在 |

---

### 4.4 创建文章

- **方法**：`POST`
- **路径**：`/api/v1/articles`
- **鉴权**：**是**（Bearer Token）
- **入参**（JSON Body）：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| title | string | 是 | 标题（1-200字符） |
| content | string | 是 | 正文 |
| summary | string | 否 | 摘要（最多500字符） |
| tag_names | string[] | 否 | 标签名称列表 |

- **请求示例**：

```json
{
  "title": "FastAPI 入门教程",
  "content": "FastAPI 是一个现代的 Python Web 框架...",
  "summary": "本文介绍 FastAPI 的基本用法",
  "tag_names": ["Python", "FastAPI"]
}
```

- **出参**：文章详情（含标签，无评论）

---

### 4.5 更新文章

- **方法**：`PUT`
- **路径**：`/api/v1/articles/{article_id}`
- **鉴权**：**是**（作者本人或 admin/editor）
- **入参**（JSON Body，所有字段可选）：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| title | string | 否 | 标题 |
| content | string | 否 | 正文 |
| summary | string | 否 | 摘要 |
| tag_names | string[] | 否 | 标签名称列表 |

- **出参**：更新后的文章详情

- **错误响应**：

| HTTP状态码 | code | message |
|------------|------|---------|
| 403 | 403 | 无权修改他人文章 |
| 404 | 404 | 文章不存在 |

---

### 4.6 删除文章

- **方法**：`DELETE`
- **路径**：`/api/v1/articles/{article_id}`
- **鉴权**：**是**（作者本人或 admin）
- **出参**：

```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

- **错误响应**：

| HTTP状态码 | code | message |
|------------|------|---------|
| 403 | 403 | 无权删除他人文章 |
| 404 | 404 | 文章不存在 |

---

### 4.7 创建评论

- **方法**：`POST`
- **路径**：`/api/v1/articles/{article_id}/comments`
- **鉴权**：**是**（Bearer Token）
- **入参**（JSON Body）：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| content | string | 是 | 评论内容（1-1000字符） |

- **请求示例**：

```json
{
  "content": "这是一条评论"
}
```

- **出参**：

```json
{
  "code": 200,
  "message": "评论成功",
  "data": {
    "id": 1,
    "content": "这是一条评论",
    "article_id": 1,
    "author_id": 2,
    "author_name": "testuser",
    "created_at": "2025-01-01T00:00:00"
  }
}
```

- **错误响应**：

| HTTP状态码 | code | message |
|------------|------|---------|
| 404 | 404 | 文章不存在 |

---

### 4.8 删除评论

- **方法**：`DELETE`
- **路径**：`/api/v1/articles/{article_id}/comments/{comment_id}`
- **鉴权**：**是**（评论作者或 admin）
- **出参**：

```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

- **错误响应**：

| HTTP状态码 | code | message |
|------------|------|---------|
| 403 | 403 | 无权删除他人评论 |
| 404 | 404 | 评论不存在 |

---

## 接口总览

| # | 方法 | 路径 | 鉴权 | 说明 |
|---|------|------|------|------|
| 1 | GET | /health | 无 | 健康检查 |
| 2 | GET | / | 无 | 根路径 |
| 3 | POST | /api/v1/auth/register | 无 | 用户注册 |
| 4 | POST | /api/v1/auth/login | 无 | 登录(OAuth2表单) |
| 5 | POST | /api/v1/auth/login/json | 无 | 登录(JSON) |
| 6 | POST | /api/v1/auth/refresh | 无 | 刷新Token |
| 7 | GET | /api/v1/auth/me | Bearer | 当前用户 |
| 8 | GET | /api/v1/users | admin | 用户列表 |
| 9 | POST | /api/v1/users | admin | 创建用户 |
| 10 | GET | /api/v1/users/{id} | admin | 用户详情 |
| 11 | PUT | /api/v1/users/{id} | admin | 更新用户 |
| 12 | DELETE | /api/v1/users/{id} | admin | 删除用户 |
| 13 | GET | /api/v1/articles | 无 | 文章列表 |
| 14 | GET | /api/v1/articles/tags | 无 | 标签列表 |
| 15 | GET | /api/v1/articles/{id} | 无 | 文章详情 |
| 16 | POST | /api/v1/articles | Bearer | 创建文章 |
| 17 | PUT | /api/v1/articles/{id} | Bearer | 更新文章 |
| 18 | DELETE | /api/v1/articles/{id} | Bearer | 删除文章 |
| 19 | POST | /api/v1/articles/{id}/comments | Bearer | 创建评论 |
| 20 | DELETE | /api/v1/articles/{aid}/comments/{cid} | Bearer | 删除评论 |
